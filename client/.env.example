# Base URL of the Dinkboard API (trailing slash recommended).
# Default server port is 5000; use 5001 if AirPlay holds 5000 on macOS.
VITE_API_BASE_URL=http://localhost:5001/

# For Docker Compose with nginx /api proxy, leave empty or unset:
# VITE_API_BASE_URL=
